{
  "name": "@subs/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared pipeline modules: Tencent signing, streaming translator, decimator, transcript, recorder, settings schema",
  "main": "index.js",
  "scripts": { "test": "node --test" },
  "dependencies": { "ws": "^8.21.3" }
}
